# Grid configuration — edit these for your cluster and project.
# Environment variables override any value set here.

GRID_EMAIL="${GRID_EMAIL:-lucas.desa@uni-heidelberg.de}"
MESA_ACTIVATE_CMD="${MESA_ACTIVATE_CMD:-mesa-24031}"
SLURM_PARTITION_SINGLE="${SLURM_PARTITION_SINGLE:-cpu-single}"
CPUS_PER_TASK="${CPUS_PER_TASK:-16}"
ARRAY_WALLTIME="${ARRAY_WALLTIME:-12:00:00}"
ARRAY_CONCURRENCY="${ARRAY_CONCURRENCY:-60}"
MAX_ARRAY_TASKS="${MAX_ARRAY_TASKS:-1000}"
